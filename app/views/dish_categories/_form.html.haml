= form_for @dish_category do |f|
  -if @dish_category.errors.any?
    #error_explanation
      %h2= "#{pluralize(@dish_category.errors.count, "error")} prohibited this dish_category from being saved:"
      %ul
        - @dish_category.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :name
    = f.text_field :name
  .actions
    = f.submit 'Save'
