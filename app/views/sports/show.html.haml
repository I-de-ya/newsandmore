.left_column
	.movie
		%h3#dark_text= "Трансляции. #{@sport.title}"
		%hr
		= render "layouts/menu"
		%table
			- @sport.broadcasts.each do |broadcast|
				%tr
					%td#dark_text= broadcast.showtime.strftime("%d/%m/%Y")
					%td#light_text= broadcast.title
			

.right_column
	= image_tag("rails.png")
	Подпись
	= render "events/archive"