.menu
  %ul
    %span#back
      = image_tag('menu_point.png')
      %li
        %b= link_to "Назад в главное меню", main_path
    - @sections.each do |s|
      %li{:class => current_path(section_path(s))}
        %b= link_to s.title, section_path(s)