.menu
  %ul
    %li
      = link_to image_tag('menu_point.png')+t(:back_to_main), main_path
    - @sections.each do |s|
      %li{:class => current_path(section_path(s))}
        = link_to s.title, section_path(s)
.clear