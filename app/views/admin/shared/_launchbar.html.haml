-# @active = :shop if ["products", "categories", "brands", "orders", "sales", "showcases"].include? controller.controller_name
- @active = :menu if %w{sections dish_categories dishes dish_types}.include? controller.controller_name
- @active = :materials if %w{events posts movies reservations sports broadcasts sliders users images settings recalls texts notes}.include? controller.controller_name

#launchbar
  %ul.menus
    %li{:class => "#{'active' if  @active == :menu}"}= link_to "Меню", [:admin,:sections]
    %li{:class => "#{'active' if  @active == :materials}"}= link_to "Материалы", [:admin,:events]
    %li.fr= Russian.strftime(Time.now,"%d %b, %Y %H:%M ")