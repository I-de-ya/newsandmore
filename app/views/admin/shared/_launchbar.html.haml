-# @active = :shop if ["products", "categories", "brands", "orders", "sales", "showcases"].include? controller.controller_name
- @active = :menu if %w{events views}.include? controller.controller_name
- @active = :materials if %w{articles reports slaiders pages users images settings}.include? controller.controller_name

#launchbar
  %ul.menus
    %li{:class => "#{'active' if  @active == :menu}"}= link_to "Меню", [:admin,:events]
    %li{:class => "#{'active' if  @active == :materials}"}= link_to "Материалы", "#"
    %li.fr= Russian.strftime(Time.now,"%d %b, %Y %H:%M ")